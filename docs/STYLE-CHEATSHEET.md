# 스타일 치트시트

작업 세션마다 이 파일 + [VOICE.md](VOICE.md) + 완성된 `ko/ch01_why.md` §1.2를 첨부합니다.
전문은 [STYLE.md](STYLE.md).

## 문단을 쓴 뒤 두 번 묻습니다

1. **이식성** — 다른 장, 다른 분야 교재에 붙여도 말이 되는가? → 되면 삭제
2. **정보량** — 숫자·고유명사·코드 식별자·파일명을 지우면 뭐가 남는가? → 없으면 삭제

## 문체

- **합니다체 통일.** `~합니다` / `~입니다` / `~까요?` / `~해 봅시다`
- 금지: `~한다` `~이다` `~하자` `~하세요`

## 장 첫머리와 개념 설명

장 첫머리는 다루는 대상의 **정의와 용도**부터 설명합니다. 실제 활용처와 이 장에서 배울 범위를 잇습니다.
데이터 표준을 다루면 구성 파일, 각 파일의 정보와 대표 필드, 식별자 관계를 실습 전에 정리합니다.
GTFS라면 정의·Google 지도 등의 활용처 → 파일 구성 → 각 표의 예시 행 → 표 연결 → 시간표 작성 순서입니다.

개별 개념은 구체적 질문 → 작은 표·도식과 손계산 → 결과 예상 → 노트북 실행·대조 → 조건 변경 순서로 설명합니다.
본문은 원리와 해석을, 노트북은 실행 코드를 중심으로 씁니다. 실습에서 사용할 새 개념을 코드로 먼저 던지지 않습니다.
수업 회차·시간 배분·수업 종료 안내는 본문과 노트북에 넣지 않습니다.

## 즉시 삭제

| | |
|---|---|
| 격상 어휘 | 혁신적, 패러다임, 핵심 메시지, 시급, 대두, 부각, 획기적, 게임 체인저 |
| 근거 없는 예측 | ~할 것으로 예상됩니다, ~될 전망입니다, 이미 입증되었습니다 |
| 형용사 삼단 나열 | "효율적이고, 지속 가능하며, 공평한" |
| 부정 정의 | "단순한 X가 아니라 Y입니다" |
| 대칭 장단점 불릿 | `- **장점**: … / - **단점**: …` |
| 빈 마무리 | 문단 끝 문장에 숫자·백틱·고유명사가 없으면 지웁니다 |
| 접속·수식 부사 | 또한, 더욱이, 나아가, 이를 통해, 매우, 다양한, 효과적으로 |
| 번역투 | ~되어진, ~에 있어서, ~을 가지고 있다, ~을 통해서 |

## 예산

- 볼드: 1000자당 3개 · 절당 문장 볼드 1개 · 최대 40자 (용어는 12자 이하 명사)
- `<u>`: 전면 금지
- admonition: 절당 1개, `note`/`tip`/`warning` 3종만
- 문장: 85자 경고 / 120자 오류
- `의` 연속 2개까지

## 표기

- 함수·컬럼·파일·패키지 이름은 **반드시** 백틱
- 최초 1회 `한국어(english)`, 이후 한국어만
- 배차(디스패치 X) · 재배치 · 보정 · 통행(여행 X) · 노드/엣지 · 공차 운행
- 인사이트 X, 솔루션 X
- 절 제목은 개념·분석 대상을 나타내는 한국어 명사구로 씁니다. GTFS·RAPTOR 등 고유 명칭은 유지합니다.
- 질문형·대화체·활동 지시형 제목을 피합니다. `승객 한 명 따라가기` → `탑승 가능 운행과 도착시각 계산`

## 그림·코드

- `{image}` 금지 → `{figure}` + `:name:` + 캡션. 본문에서 참조하지 않는 그림은 삭제
- raw HTML 레이아웃(`<div style=`, `<p style=`) 금지
- 코드 셀: 위에 왜 하는지 1~3문장, 아래에 출력 해석 1~2문장. 해석 없는 셀은 미완성

## 작업 규율

- **한 세션에 한 절(`##`)만.** 문체는 인접 문장에서 전염됩니다
- 커밋 전 `python tools/kolint.py <파일>` 통과
- 커밋 메시지에 **무엇을 삭제했는지** 적습니다
- 1장은 15,320자 → 9,000자 안팎으로 줄어드는 게 정상. 늘었으면 뭔가 잘못됐습니다
